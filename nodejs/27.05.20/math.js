// exports.sum = (a, b) => a + b;
// exports.sub = (a, b) => a - b;
// exports.mult = (a, b) => a * b;
// exports.div = (a, b) => a / b;

// module.exports = {
//   sum: (a, b) => a + b,
//   sub: (a, b) => a - b,
//   mult: (a, b) => a * b,
//   div: (a, b) => a / b,
// };

sum = (a, b) => a + b;
sub = (a, b) => a - b;
mult = (a, b) => a * b;
div = (a, b) => a / b;

module.exports = {
  sum,
  sub,
  mult,
  div,
};
